# TODO - SettingsForm component + tests

- [ ] Rewrite `src/components/SettingsForm.jsx` to match exact specs (Tailwind-only styling, required fields with * , inline errors, aria-invalid/aria-describedby, email +admin rejection, timezone select w/ optimistic UI, bio char count, success message 3s).
- [ ] Add test tooling (Vitest + React Testing Library + jsdom) and update `package.json` scripts.
- [ ] Create `src/components/SettingsForm.test.jsx` with at least 5 tests matching requirements.
- [ ] Run tests; if failing, fix component (not tests) and re-run until passing.
- [ ] Confirm terminal output showing tests passing.

